//console.log(!true)

const digiteasenha = false
const digitetoken = false
const digetenúmerodaconta = true


/* if   (digiteasenha || digitetoken || digetenúmerodaconta){ console.log("Logado com sucesso")

 }

  else     { console.log("Não logado informação errada") }*/

  if   (digiteasenha && digitetoken && digetenúmerodaconta){ console.log("Logado com sucesso")

  }
 
   else     { console.log("Não logado informação errada") }

